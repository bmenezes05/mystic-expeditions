@page "/newgame"
@using MysticExpeditions.Client.Services
@inject NavigationManager NavigationManager
@inject GameStateService GameStateService

<div class="new-game-container">
    <h1>Create a New Character</h1>

    <label for="playerName">Name:</label>
    <input type="text" id="playerName" @bind="playerName" />

    <!-- Adicione outras opções de atributos do personagem aqui -->

    <button @onclick="StartGame">Start Game</button>
</div>

@code {
    private string playerName;

    private void StartGame()
    {
        GameStateService.PlayerName = playerName;
        GameStateService.PlayerHealth = 100;
        GameStateService.Inventory.Clear();
        NavigationManager.NavigateTo("/game"); // Exemplo simples, ajuste conforme sua lógica
    }
}

<style>
    .new-game-container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
    }

        .new-game-container h1 {
            text-align: center;
        }

        .new-game-container label {
            display: block;
            margin-bottom: 10px;
        }

        .new-game-container input, .new-game-container select {
            width: 100%;
            padding: 8px;
            font-size: 16px;
            margin-bottom: 10px;
        }

        .new-game-container button {
            padding: 10px 20px;
            font-size: 18px;
        }
</style>